export default {
  /* Where `build` writes the plugin folder. */
  out: "plugin",
  /* Extra arguments for the `yeet run` the plugin's QML starts, e.g.
   * ["--heap-limit", "1GiB"]. The app path is appended after them. */
  // yeetArgs: [],
};
